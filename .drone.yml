--- 
clone: 
  depth: 1
kind: pipeline
name: ReinaPipeline
steps: 
  - 
    commands: 
      - "apt-get update && DEBIAN_FRONTEND=noninteractive apt-get install git sudo -y && bash komodo.sh"
    image: ubuntu:latest
    name: Zhard-Lair
    trigger: 
      branch: 
        - master
